apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: keycloak-operator
  namespace: flux-system
spec:
  interval: 10m
  path: ./clusters/my-cluster/infrastructure/keycloak-operator
  prune: true
  sourceRef:
    kind: GitRepository
    name: platform-config
  wait: true
